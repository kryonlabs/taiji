#!/bin/rc
# Windows-2000 style Run... for the TaijiOS desktop: read a command,
# then open it in a new window via wctl.

echo 'Type the name of a program, and TaijiOS will open it for you.'
echo -n 'Run: '
cmd=`{read}
if(! ~ $#cmd 0)
	window $cmd
if not
	echo 'q9run: no command given' >[1=2]

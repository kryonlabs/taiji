#!/bin/rc
# Windows-2000 style Run... for rio: read a command, open it in a
# new window via wctl.

echo 'Type the name of a program, and Plan 9 will open it for you.'
echo -n 'Run: '
cmd=`{read}
if(! ~ $#cmd 0)
	echo new $cmd >/dev/wctl
if not
	echo 'q9run: no command given' >[1=2]
